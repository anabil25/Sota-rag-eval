FROM python:3.11-slim

ENV PYTHONDONTWRITEBYTECODE=1 \
    PYTHONUNBUFFERED=1 \
    PORT=8080

WORKDIR /app

COPY pyproject.toml ./
COPY src ./src

RUN pip install --no-cache-dir "."

EXPOSE 8080

CMD ["uvicorn", "retrieve.azure_openai_proxy.app:app", "--host", "0.0.0.0", "--port", "8080"]